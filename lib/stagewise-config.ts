/**
 * Stagewise configuration for the toolbar
 * This file provides the proper configuration with React plugin integration
 */

import ReactPlugin from '@stagewise-plugins/react'

export const stagewiseConfig = {
  plugins: [ReactPlugin]
}
